/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Temporary escape hatch if TS errors block builds:
  // typescript: { ignoreBuildErrors: true },
};

export default nextConfig;
